export default {
    methods: {
        /**
         * 设置页面标题
         *
         * @param {string} title
         */
        setTitle(title) {
            MXWebui.setWebViewTitle(title);
        }
    }
}